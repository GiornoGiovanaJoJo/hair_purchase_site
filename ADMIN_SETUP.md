# Custom Admin Interface v3 - Полная Документация

## Что Добавлено

### 1. Dashboard (Главная Страница Админки)
- Статистика по заявкам (всего, новые, принятые, завершённые, отклонённые)
- Финансовые метрики (смета, финальная цена, средняя цена)
- График динамики заявок за 30 дней (Chart.js)
- Список последних 10 заявок
- Кнопки для экспорта CSV/Excel

### 2. Таблицы с Красивым Дизайном
- Цветные статус-бейджи
- Профессиональные иконки для данных
- Быстрые действия (изменить статус, отклонить, завершить)
- Продвинутые фильтры
- Поиск по имени, телефону, email

### 3. Экспорт Данных
- **CSV экспорт** - все заявки с полной информацией
- **Excel экспорт** - красивый формат с стилями и форматированием
- **Цены Excel** - отдельный экспорт прайс-листа

### 4. Цветовая Схема
```
#1a1a2e  - Primary (очень тёмно-синий)
#16213e  - Secondary (тёмно-синий)
#0f3460  - Accent (синий)
#e94560  - Danger (красный)
#27ae60  - Success (зелёный)
#f39c12  - Warning (оранжевый)
#3498db  - Info (светло-синий)
```

## Установка

### 1. Установить зависимость
```bash
pip install openpyxl
```

Или обновить всё сразу:
```bash
pip install -r requirements.txt
```

### 2. Обновить Django
```bash
python manage.py migrate
python manage.py collectstatic --noinput
```

### 3. Перезагрузить сервер

**На локальной машине:**
```bash
python manage.py runserver
# Открыть http://localhost:8000/admin/
```

**На сервере:**
```bash
ssh -p 49196 root@4895c9d9450e.vps.myjino.ru

cd /opt/hair_purchase_site

# Обновить код
git pull origin main

# Установить зависимости
pip install openpyxl

# Перезагрузить Django
sudo systemctl restart hair_purchase

# Проверить логи
journalctl -u hair_purchase -n 20 --no-pager
```

## Файлы

### Новые файлы в `hair_app/`:

1. **`admin_views.py`** - Функции для получения статистики
   - `get_dashboard_stats()` - Главная статистика
   - `get_chart_data()` - Данные для графика
   - `get_recent_applications()` - Последние заявки
   - `get_price_stats()` - Статистика по ценам

2. **`admin_utils.py`** - Утилиты для экспорта
   - `export_applications_to_csv()` - CSV экспорт заявок
   - `export_applications_to_excel()` - Excel экспорт заявок
   - `export_prices_to_excel()` - Excel экспорт цен

3. **`admin_views_export.py`** - Views для экспорта
   - `export_applications_csv()` - View для CSV
   - `export_applications_excel()` - View для Excel
   - `export_prices_excel()` - View для цен

4. **`admin.py`** - Обновленный файл админки
   - Улучшенные display методы
   - Цветные бейджи статусов
   - Красивые иконки и форматирование

5. **`templates/admin/custom_dashboard.html`** - Шаблон dashboard
   - Сетка статистики
   - График Chart.js
   - Список последних заявок
   - Стили и адаптив

### Обновленные файлы:

1. **`config/urls.py`** - Добавлены экспорт URL'ы
2. **`requirements.txt`** - Добавлен openpyxl

## Использование

### 1. Открыть Админку
```
http://ваш-сайт.ru/admin/
```

### 2. Главная Страница
Видите статистику, график и последние заявки

### 3. Экспортировать Данные

**CSV:**
```
http://ваш-сайт.ru/admin/export/applications/csv/
```

**Excel (Заявки):**
```
http://ваш-сайт.ru/admin/export/applications/excel/
```

**Excel (Цены):**
```
http://ваш-сайт.ru/admin/export/prices/excel/
```

### 4. Фильтровать Заявки
- По статусу (Новая, Принята, Завершена, Отклонена)
- По длине волос
- По цвету
- По структуре
- По дате создания

### 5. Поиск
Ищите по:
- ID заявки
- Имени клиента
- Телефону
- Email

## Функциональность

### Dashboard Статистика
- **Всего заявок** - общее количество
- **Новые** - требуют внимания
- **Принято** - в работе
- **Завершено** - успешно
- **Отклонено** - не подходит
- **Средняя цена** - среднее значение
- **Смета всего** - сумма всех смет
- **Выплачено** - сумма завершённых

### Действия в Таблице
- ✅ Принять заявку
- ❌ Отклонить заявку
- ✔️ Завершить заявку
- 📝 Редактировать данные
- 🔍 Фильтровать

## Примеры

### Быстро изменить статус нескольких заявок
1. Отметить checkbox рядом с заявками
2. Выбрать действие из dropdown ("Принять выбранные", "Отклонить выбранные", "Завершить выбранные")
3. Нажать "Применить"

### Экспортировать все заявки за месяц
1. Нажать фильтр по дате
2. Выбрать период
3. Нажать кнопку "Excel" или "CSV"
4. Файл скачается

### Найти заявку клиента
1. В поле поиска написать имя, телефон или email
2. Нажать Enter
3. Результаты отфильтруются

## Проверка

### Локально
```bash
# Тест статистики
python manage.py shell
>>> from hair_app.admin_views import get_dashboard_stats
>>> stats = get_dashboard_stats()
>>> print(stats)
{'total': 42, 'new': 5, 'accepted': 15, ...}

# Тест графика
>>> from hair_app.admin_views import get_chart_data
>>> data = get_chart_data()
>>> print(data['labels'][:5])  # Первые 5 дат
```

### На Сервере
```bash
# Проверить логи админки
journalctl -u hair_purchase -n 50 --no-pager | grep -i admin

# Проверить доступ к админке
curl -I http://4895c9d9450e.vps.myjino.ru/admin/
# Должно вернуть 200 или 301
```

## Проблемы и Решение

### Ошибка: "No module named 'openpyxl'"
```bash
pip install openpyxl
```

### Ошибка: "Template not found"
Убедитесь что `custom_dashboard.html` в правильной папке:
```
hair_app/templates/admin/custom_dashboard.html
```

### Экспорт не работает
1. Проверить что вы залогинены в админку
2. Проверить что вы staff member (`User.is_staff = True`)
3. Проверить логи:
```bash
journalctl -u hair_purchase -n 20 --no-pager
```

### График не отображается
1. Проверить что подключена Chart.js в template
2. Проверить браузер консоль (F12 -> Console) на ошибки
3. Проверить что JSON данные передаются:
```js
console.log({{ chart_data|safe }})
```

## Развитие

Возможные улучшения:
1. Добавить PDF экспорт
2. Расширить аналитику (графики по цветам, структурам)
3. Кастомные отчёты по датам
4. Email рассылка статистики
5. Mobile-приложение для админа

## Поддержка

Если что-то не работает:
1. Проверьте логи сервера
2. Проверьте что все файлы загружены на GitHub
3. Проверьте что `pip install -r requirements.txt` запущена
4. Перезагрузите сервер: `sudo systemctl restart hair_purchase`

---

**Версия:** v3.0.0  
**Дата:** 16 декабря 2025  
**Статус:** Production Ready ✅
